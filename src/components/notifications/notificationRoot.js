import React, {Component} from 'react';
import PropTypes from 'prop-types';

//Call relevant Data from the interactions collection and format it appropratiely into the notification Component
class NotificationRoot extends Component {
    render() {
        return (
            <div>

            </div>
        );
    }
}

NotificationRoot.propTypes = {};

export default NotificationRoot;
