import React, {Component} from 'react';
import PropTypes from 'prop-types';
import {View, FlatList} from 'react-native'

class FeedRootComponent extends Component {
    render() {
        return (
          null
        );
    }
}

FeedRootComponent.propTypes = {};

export default FeedRootComponent;
